import { ForbitNameDirective } from './forbit-name.directive';

describe('ForbitNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbitNameDirective();
    expect(directive).toBeTruthy();
  });
});
